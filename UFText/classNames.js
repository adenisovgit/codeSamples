import {
  baseClassName as richTextEditorBaseCN,
  quillEditor,
} from '@organisms/RichTextEditor/classNames';

export const baseClassName = 'UFText';

export const quillEditorClassName = quillEditor;
export const richTextEditorBaseClassName = richTextEditorBaseCN;
